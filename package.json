{
  "name": "zmsy",
  "version": "1.0.0",
  "description": "Personal website for Zach Morrissey.",
  "author": "Zach Morrissey",
  "license": "MIT",
  "dependencies": {
    "bulma": "^0.8.2"
  },
  "devDependencies": {
    "@babel/core": "^7.11.1",
    "@babel/preset-env": "^7.11.0",
    "babel-loader": "^8.1.0",
    "css-loader": "^3.6.0",
    "cssnano": "^4.1.10",
    "file-loader": "^6.0.0",
    "live-server": "^1.2.1",
    "mini-css-extract-plugin": "^0.9.0",
    "node-sass": "^4.14.1",
    "postcss-loader": "^3.0.0",
    "postcss-preset-env": "^6.7.0",
    "sass-loader": "^8.0.2",
    "webpack": "^4.44.1",
    "webpack-cli": "^3.3.12",
    "webpack-dev-server": "^3.11.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "run-p dev:**",
    "dev:webpack": "webpack-dev-server --hot",
    "dev:hugo": "hugo server",
    "preview": "run-p preview:**",
    "preview:hugo": "npm run dev:hugo -- -D -F",
    "preview:webpack": "npm run dev:webpack",
    "build-dev": "webpack && hugo -v --minify",
    "build": "NODE_ENV=production webpack -p && NODE_ENV=production hugo -v --minify",
    "build-ci": "NODE_ENV=production webpack -p && NODE_ENV=production hugo-official -v --minify",
    "deploy-ci": "hugo-official -v deploy --maxDeletes -1 --invalidateCDN"
  }
}
